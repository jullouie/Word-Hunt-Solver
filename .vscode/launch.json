{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Launch WordSolver",
            "request": "launch",
            "mainClass": "src.WordSolver.WordSolver",
            "cwd": "${workspaceFolder}",
            "classPaths": [
                "${workspaceFolder}/bin"
            ]
        }
    ]
}
